<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.professor.app">

    <!-- صلاحيات الإنترنت والظهور فوق التطبيقات والإشعارات -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- صلاحيات نظام التنبيهات الفورية: إيقاظ الجهاز + إقلاع تلقائي + استثناء البطارية -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- صلاحيات قراءة وكتابة الملفات (ضرورية للنسخ الاحتياطي والصور وتصدير PDF) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/professor_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/professor_logo"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FileChooserActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:exported="false" />

        <activity
            android:name=".ProfessorFloatingLauncherActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:exported="false" />

        <service
            android:name=".ProfessorBubbleService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating PROFESSOR trading panel" />
        </service>

        <!-- خدمة التنبيهات الاحتياطية المستقلة (تعمل حتى بلا خدمات جوجل) -->
        <service
            android:name=".ProfessorNotificationService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- مستقبل قناة الدفع الرسمية من جوجل (FCM) — يعمل حتى والتطبيق مقفول -->
        <service
            android:name=".ProfessorFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".ProfessorTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_professor"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- إقلاع تلقائي: إعادة تشغيل نظام التنبيهات بعد تشغيل الهاتف -->
        <receiver
            android:name=".ProfessorBootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- مؤقّت الإيقاظ الدوري (يكسر وضع Doze) -->
        <receiver
            android:name=".ProfessorKeepAliveReceiver"
            android:exported="false" />
    </application>
</manifest>
